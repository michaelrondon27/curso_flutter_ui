import 'package:flutter/material.dart';

final miTema = ThemeData.dark().copyWith(
  scaffoldBackgroundColor: Color(0xff000003),
  iconTheme: IconThemeData(
    color: Colors.white.withOpacity(0.4)
  )
);
